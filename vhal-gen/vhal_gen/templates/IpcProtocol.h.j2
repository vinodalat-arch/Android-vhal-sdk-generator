/*
 * IPC protocol definitions for Vehicle bridge communication.
 * Shared between BridgeVehicleHardware (VHAL process) and VehicleDaemon.
 * DO NOT EDIT -- generated by vhal-gen.
 */
#pragma once

#include <cstddef>
#include <cstdint>

namespace vehicle {

enum class IpcMsgType : uint32_t {
    SET_PROPERTY = 1,
    GET_PROPERTY = 2,
    PROPERTY_CHANGED = 3,
    PROPERTY_RESPONSE = 4,
};

enum class IpcValueType : uint32_t {
    BOOLEAN = 1,
    INT32 = 2,
    FLOAT = 3,
    INT64 = 4,
};

struct __attribute__((packed)) IpcMessage {
    IpcMsgType msg_type;
    int32_t property_id;
    int32_t area_id;
    IpcValueType value_type;
    uint32_t value_len;
    uint8_t value[];  // flexible array member (C99/C++ ext)
};

constexpr size_t IPC_HEADER_SIZE = offsetof(IpcMessage, value);

}  // namespace vehicle
